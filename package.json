{
  "name": "binary-parser",
  "description": "Parsing binary data by rules",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "gypfile": true,
  "scripts": {
    "install": "node-gyp rebuild && tsc",
    "prepublish": "tsc",
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@types/node": "^12.12.21",
    "jest": "^24.9.0",
    "typescript": "^3.7.4"
  },
  "devDependencies": {
    "@types/jest": "^24.0.24",
    "ts-jest": "^24.2.0"
  }
}
